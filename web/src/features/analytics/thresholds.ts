// Mirrors `api/app/core/constants.py` - kept in sync by hand since the two
// stacks don't share a module, same as every other cross-stack constant in
// this codebase (e.g. `MAX_REPORTING_DEPTH`).
export const WIDE_SPAN_THRESHOLD = 10
export const DEEP_CHAIN_THRESHOLD = 6
